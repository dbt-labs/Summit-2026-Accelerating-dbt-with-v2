models:
  - name: task_2_fct_store_daily_buggy
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - store_id
              - order_date
    columns:
      - name: store_id
        data_tests:
          - not_null
      - name: order_date
        data_tests:
          - not_null
